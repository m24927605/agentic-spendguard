//! Anthropic Claude 3 / 3.5 BPE encoder.
//!
//! Lands in Phase B. Phase A ships the module path stub so the
//! refactor commit is self-contained — the implementation arrives
//! together with the vendored asset + tokenizers-crate dependency.
