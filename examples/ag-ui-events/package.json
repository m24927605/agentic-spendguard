{
  "name": "spendguard-ag-ui-events-example",
  "version": "0.1.0",
  "private": true,
  "description": "Runnable Node demo: a SpendGuard-guarded agent run (real sidecar UDS, real ledger) that emits the spendguard.* AG-UI CUSTOM display events over SSE. Display-only — enforcement stays in the SpendGuard adapters and sidecar.",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=20.10.0"
  },
  "scripts": {
    "start": "node index.mjs"
  },
  "dependencies": {
    "@spendguard/ag-ui": "^0.1.0",
    "@spendguard/sdk": "^0.5.0"
  }
}
