---
title: "Tabnine Enterprise with SpendGuard"
description: >-
  Drop SpendGuard into Tabnine Enterprise by configuring the BYO LLM
  endpoint URL in the admin UI to point at a running SpendGuard egress
  proxy. Recipe in progress; the exact UI step is on the drop-in landing.
pagefind: false
---

## Coming soon

The full walkthrough — prerequisites, the exact admin UI step, the
verification step, and gotchas — lands in a follow-up slice. In the
meantime, the setting to use is row 9 of the
[drop-in landing matrix](../#find-your-tool): in the Tabnine Enterprise
admin UI, set the BYO LLM endpoint URL to `http://localhost:9000/v1`.

Source: [Tabnine connect custom LLM](https://docs.tabnine.com/main/software-configuration/connect-custom-llm).
