---
title: "Drop in SpendGuard in 30 seconds (14 tools, one env var)"
description: >-
  Every tool that already accepts an OpenAI-compatible base URL — LiteLLM
  proxy, Aider, Continue, Cline / Roo Code, OpenHands, Goose, Zed AI,
  GitHub Copilot CLI, Tabnine Enterprise, AnythingLLM, LobeChat, Cody
  self-hosted, Augment, Dify — works with Agentic SpendGuard out of the
  box. One env var, no SDK install, no code change.
---

import DropInPicker from '../../../../components/DropInPicker.astro';

If your agent stack already uses an OpenAI-compatible client, SpendGuard
drops in by changing one base URL. Start the SpendGuard egress proxy at
`http://localhost:9000/v1`, point your tool at it, and every model call
flows through SpendGuard's pre-call budget gate and KMS-signed audit
chain before it ever hits the upstream provider. No SDK install, no code
change, no rebuild. Find your tool in the matrix below, copy the line
your tool's docs specify, and the next call is governed.

## How Pattern 2 works

Each of these 14 tools already supports a custom OpenAI-compatible base
URL. SpendGuard runs an OpenAI-compatible endpoint of its own; setting
the env var (or one config line) routes the tool's traffic through
SpendGuard before it reaches OpenAI / Anthropic / Bedrock. Pattern 2
contrasts with Pattern 1 (in-process SDK middleware, used for the
adapter integrations) and Pattern 3 (egress proxy + CA install, used
for closed BYOK CLIs like Claude Code and Codex).

## Start the proxy locally (30 seconds)

```bash
git clone https://github.com/m24927605/agentic-spendguard.git
cd agentic-spendguard
export OPENAI_API_KEY=sk-...
make demo-up DEMO_MODE=proxy
```

The egress proxy binds on `http://localhost:9000/v1`. Point any of the
14 tools below at that URL.

## Find your tool

The matrix lists the exact env var name or config key each tool's
maintainer docs specify. The `Verified` column distinguishes rows we
have run end-to-end against the egress proxy (`Live`), rows whose
configuration matches the maintainer's published docs but we have not
ourselves smoke-tested (`Spec`), and rows the tool's own changelog
documents natively without SpendGuard-specific testing (`Vendor-native`).

Use the quick filter below to narrow by setting type (env var, config
file, or admin UI), search by tool name, and jump straight to the
per-tool section.

<DropInPicker />

The static matrix below carries the upstream-source links and the row
numbering the rest of this page references; the picker above is a
quick-filter layer over the same data.

| # | Tool | Provider field | Exact value | Upstream source | Verified | Recipe |
|---|------|----------------|-------------|-----------------|----------|--------|
| 1 | [LiteLLM (proxy mode)](#litellm) | Env var | `OPENAI_API_BASE=http://localhost:9000/v1` | [docs.litellm.ai](https://docs.litellm.ai/docs/proxy/config_settings) | Spec | [Open ↗](./litellm/) |
| 2 | [Aider](#aider) | Env var | `OPENAI_API_BASE=http://localhost:9000/v1` | [aider.chat](https://aider.chat/docs/llms/openai-compat.html) | Spec | [Open ↗](./aider/) |
| 3 | [Continue](#continue) | YAML `apiBase` | `apiBase: http://localhost:9000/v1` | [docs.continue.dev](https://docs.continue.dev/customization/models) | Spec | [Open ↗](./continue/) |
| 4 | [Cline / Roo Code (BYOK)](#cline-roo-code) | UI field | Custom OpenAI provider, Base URL `http://localhost:9000/v1` | [docs.cline.bot](https://docs.cline.bot/getting-started/byok) | Spec | [Open ↗](./cline-roo-code/) |
| 5 | [OpenHands (BYOK)](#openhands) | UI field | LLM custom endpoint `http://localhost:9000/v1` | [docs.all-hands.dev](https://docs.all-hands.dev/usage/llms/custom-llm-configs) | Spec | [Open ↗](./openhands/) |
| 6 | [Goose](#goose) | Env var | `OPENAI_HOST=http://localhost:9000` | [block.github.io/goose](https://block.github.io/goose/docs/getting-started/installation) | Spec | [Open ↗](./goose/) |
| 7 | [Zed AI](#zed) | TOML key | `api_url = "http://localhost:9000/v1"` | [zed.dev](https://zed.dev/docs/ai/configuration) | Vendor-native | [Open ↗](./zed/) |
| 8 | [GitHub Copilot CLI (BYOK)](#copilot-cli) | Env var | `COPILOT_PROVIDER_BASE_URL=http://localhost:9000/v1` | [docs.github.com](https://docs.github.com/en/copilot/github-copilot-cli/using-byok) | Vendor-native | [Open ↗](./copilot-cli/) |
| 9 | [Tabnine Enterprise](#tabnine) | Admin UI | BYO LLM endpoint `http://localhost:9000/v1` | [docs.tabnine.com](https://docs.tabnine.com/main/software-configuration/connect-custom-llm) | Spec | [Open ↗](./tabnine/) |
| 10 | [AnythingLLM](#anythingllm) | Admin UI | Generic OpenAI provider, Base URL `http://localhost:9000/v1` | [docs.anythingllm.com](https://docs.anythingllm.com/llm-configuration/custom-openai-base-url) | Live | [Open ↗](./anythingllm/) |
| 11 | [LobeChat](#lobechat) | Env var | `OPENAI_PROXY_URL=http://localhost:9000/v1` | [lobehub.com](https://lobehub.com/docs/self-hosting/environment-variables/model-provider#openai_proxy_url) | Live | [Open ↗](./lobechat/) |
| 12 | [Cody self-hosted Enterprise](#cody) | Site config | Sourcegraph relay endpoint `http://localhost:9000/v1` | [sourcegraph.com](https://sourcegraph.com/docs/cody/clients/install-vscode) | Spec | [Open ↗](./cody/) |
| 13 | [Augment (BYOK)](#augment) | UI field | LLM custom endpoint `http://localhost:9000/v1` | [docs.augmentcode.com](https://docs.augmentcode.com/setup/byok) | Spec | [Open ↗](./augment/) |
| 14 | [Dify](#dify) | Plugin manifest | Custom Model Provider plugin → base URL `http://localhost:9000/v1` | [docs.dify.ai](https://docs.dify.ai/plugins/model-provider) | Spec | [Open ↗](./dify/) |
| 15 | [CrewAI Studio (via LiteLLM)](#litellm) | Indirect | Use row 1 — point CrewAI at the LiteLLM proxy, point LiteLLM at SpendGuard | [docs.litellm.ai](https://docs.litellm.ai/docs/proxy/config_settings) | Spec | [Open ↗](./litellm/) |

Already running SpendGuard in production? Swap `http://localhost:9000/v1`
for your SpendGuard egress proxy URL — a Kubernetes Service URL, a
sidecar URL inside the pod, or a hosted SpendGuard URL. The relative
path is always `/v1`. See the [Helm deployment guide](../deployment/helm/)
for the production hostname pattern.

## Per-tool setup

Each section below is the H3 anchor the matrix above links to. Copy the
code block, set it in your shell or config, and the next call from that
tool routes through SpendGuard. The full walkthrough — verification
steps and known gotchas — lives in each tool's recipe page in the
sidebar.

<h3 id="litellm">LiteLLM</h3>

LiteLLM proxy mode reads `OPENAI_API_BASE` for the upstream endpoint
when you set the OpenAI provider with no explicit base URL on a route.
Export the var before `litellm --config config.yaml` (or set it in your
`config.yaml` environment block).

```bash
export OPENAI_API_BASE=http://localhost:9000/v1
```

Source: [LiteLLM proxy config settings](https://docs.litellm.ai/docs/proxy/config_settings).

<h3 id="aider">Aider</h3>

Aider picks up `OPENAI_API_BASE` from the environment and routes every
chat completion through it. Set the env var in the shell you launch
`aider` from.

```bash
export OPENAI_API_BASE=http://localhost:9000/v1
```

Source: [Aider OpenAI-compatible APIs guide](https://aider.chat/docs/llms/openai-compat.html).

<h3 id="continue">Continue</h3>

Continue reads `apiBase` from each model entry in `config.yaml` (under
the model's `config` block for the OpenAI provider). Edit the YAML and
reload the IDE extension.

```yaml
models:
  - name: SpendGuard-gated GPT-4o
    provider: openai
    model: gpt-4o
    config:
      apiBase: http://localhost:9000/v1
```

Source: [Continue model customization](https://docs.continue.dev/customization/models).

<h3 id="cline-roo-code">Cline / Roo Code (BYOK)</h3>

In the Cline or Roo Code extension settings, pick the **OpenAI
Compatible** provider and fill the **Base URL** field. No env var
required — this is a UI-only setting that persists in the workspace
config.

```text
Provider:  OpenAI Compatible
Base URL:  http://localhost:9000/v1
API Key:   <any value the proxy will forward upstream>
```

Source: [Cline BYOK guide](https://docs.cline.bot/getting-started/byok).

<h3 id="openhands">OpenHands (BYOK)</h3>

OpenHands' UI exposes a **Custom LLM endpoint** field under Settings →
LLM → Custom Provider. Enter the SpendGuard egress URL there; OpenHands
will use it for every model call in the workspace.

```text
Settings → LLM → Custom Provider → Endpoint
http://localhost:9000/v1
```

Source: [OpenHands custom LLM configs](https://docs.all-hands.dev/usage/llms/custom-llm-configs).

<h3 id="goose">Goose</h3>

Goose reads `OPENAI_HOST` (host, not full base URL — Goose appends
`/v1` itself for the OpenAI provider). Export the variable in the shell
you launch `goose` from.

```bash
export OPENAI_HOST=http://localhost:9000
```

Source: [Goose installation guide](https://block.github.io/goose/docs/getting-started/installation).

<h3 id="zed">Zed AI</h3>

Zed AI configuration is TOML inside `settings.json` (Zed reads JSON or
TOML depending on file extension). Add an `api_url` to the OpenAI
provider block.

```toml
[ai.openai]
api_url = "http://localhost:9000/v1"
```

Source: [Zed AI configuration](https://zed.dev/docs/ai/configuration).

<h3 id="copilot-cli">GitHub Copilot CLI (BYOK)</h3>

The GA-2026-04-07 BYOK release of `gh copilot` honours
`COPILOT_PROVIDER_BASE_URL` for the OpenAI-compatible provider. Export
the var; `gh copilot` picks it up on the next invocation.

```bash
export COPILOT_PROVIDER_BASE_URL=http://localhost:9000/v1
```

Source: [GitHub Copilot CLI BYOK reference](https://docs.github.com/en/copilot/github-copilot-cli/using-byok).

<h3 id="tabnine">Tabnine Enterprise</h3>

Tabnine Enterprise's admin UI has a **Connect Custom LLM** form for
self-hosted endpoints. Paste the SpendGuard egress URL into the
endpoint field and save; the change rolls out to every Tabnine seat
on the tenant.

```text
Admin → AI Models → Connect Custom LLM → Endpoint URL
http://localhost:9000/v1
```

Source: [Tabnine connect custom LLM](https://docs.tabnine.com/main/software-configuration/connect-custom-llm).

<h3 id="anythingllm">AnythingLLM</h3>

AnythingLLM ships a **Generic OpenAI** provider tile under
**Settings → LLM Preference**. Pick the tile, set the Base URL to your
SpendGuard egress endpoint (trailing `/v1` required), and any non-empty
API Key. The full per-tool walkthrough — four exact fields, Docker /
Desktop / Cloud notes, the `DEMO_MODE=anythingllm_real` smoke — lives in
the [AnythingLLM recipe](./anythingllm/).

```text
Settings → LLM Preference → Generic OpenAI → Base URL
http://localhost:9000/v1
```

Source: [AnythingLLM custom OpenAI base URL](https://docs.anythingllm.com/llm-configuration/custom-openai-base-url).

<h3 id="lobechat">LobeChat</h3>

LobeChat honours an `OPENAI_PROXY_URL` env var that rewrites the
OpenAI upstream for every server-side chat at `/api/chat/openai`. Set
it at container boot and every chat routes through SpendGuard with no
LobeChat code change. The full per-tool walkthrough — Docker /
Vercel / Cloud / Client-mode notes, the `DEMO_MODE=lobechat_real`
smoke — lives in the [LobeChat recipe](./lobechat/).

```text
docker run -e OPENAI_PROXY_URL=http://localhost:9000/v1 \
           -e ACCESS_CODE=... -p 3210:3210 lobehub/lobe-chat:1.40.0
```

Source: [LobeChat — `OPENAI_PROXY_URL` env var](https://lobehub.com/docs/self-hosting/environment-variables/model-provider#openai_proxy_url).

<h3 id="cody">Cody self-hosted Enterprise</h3>

Sourcegraph Cody self-hosted uses a site-config block for the
completions relay endpoint. Edit `site-config.json` to point the
relay at SpendGuard.

```json
{
  "cody.completions.endpoint": "http://localhost:9000/v1"
}
```

Source: [Cody self-hosted install guide](https://sourcegraph.com/docs/cody/clients/install-vscode).

<h3 id="augment">Augment (BYOK)</h3>

Augment's BYOK flow has an **LLM custom endpoint** field on the
workspace settings page. Paste the SpendGuard URL and save; Augment
uses it for every model call.

```text
Settings → BYOK → LLM Custom Endpoint
http://localhost:9000/v1
```

Source: [Augment BYOK setup](https://docs.augmentcode.com/setup/byok).

<h3 id="dify">Dify</h3>

Dify wires custom OpenAI-compatible endpoints through its **Model
Provider Plugin** mechanism. The plugin manifest declares the base
URL; load it into your Dify workspace and pick it as the default
provider.

```yaml
type: model-provider
provider: openai-compatible
base_url: http://localhost:9000/v1
```

Source: [Dify model provider plugin](https://docs.dify.ai/plugins/model-provider).

## What works today

The matrix above is the **drop-in surface**: tools whose maintainer docs
publicly document a custom OpenAI-compatible base URL on 2026-06-06. The
`Verified` column reflects SpendGuard's own claim per row; for the
runtime-verified client SDKs (openai-python, LangChain ChatOpenAI,
LangGraph, openai-agents shorthand, streaming) see the README's
`What works today` table.

## What to do next

- [Provision a real budget on Kubernetes](../deployment/helm/)
- [Open the SpendGuard dashboard](../operations/dashboard/)
- [Cover the rest of your stack (Pattern 3 egress proxy)](../deployment/docker-compose/)
- [Wire a framework SDK (Pattern 1)](../integrations/openai-agents/)
- [How SpendGuard compares to Cloudflare AI Gateway, Databricks Unity, Portkey](../posts/agent-spend-governance-gap/)

The per-tool deep-dive pages linked in the `Recipe` column above are
the SLICE-2 / D33 / D34 walkthroughs (prerequisites, exact config
block, verification step, gotchas). At Slice 1 each `Open ↗` link
resolves to a `Coming soon` stub; the env var or config line for that
tool is in the matrix above and repeated in the per-tool H3 sections
on this page.
