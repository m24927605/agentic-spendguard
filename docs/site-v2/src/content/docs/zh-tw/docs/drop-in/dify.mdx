---
title: "在 Dify 接上 SpendGuard"
description: >-
  在 Dify 註冊一個 custom Model Provider plugin,把 manifest 裡的
  base URL 指到正在跑的 SpendGuard egress proxy,就能把 SpendGuard
  接進來。Recipe 還在做,完整的 plugin manifest 先放在 drop-in 入口頁。
pagefind: false
---

## 即將推出

完整流程 — 前置條件、確切的 plugin manifest、驗證步驟、還有那些雷 — 會在後續的
slice 補上。在那之前,你要設的就是
[drop-in 入口對照表](../#find-your-tool)第 14 列:註冊一個 custom Model
Provider plugin,把它的 base URL 設成 `http://localhost:9000/v1` 就好。

來源:[Dify Model Provider plugin](https://docs.dify.ai/plugins/model-provider)。
