---
title: "Tabnine Enterprise 接 SpendGuard"
description: >-
  在 Tabnine Enterprise 的管理介面把 BYO LLM endpoint URL 指到一個跑著的
  SpendGuard egress proxy,就能把 SpendGuard 接進來。Recipe 還在做,
  確切的 UI 步驟先看 drop-in 對照表。
pagefind: false
---

## 即將推出

完整教學 — 前置需求、確切的管理介面步驟、驗證方式、還有踩雷提醒 —
會在後續 slice 補上。在那之前,要設的就是
[drop-in 對照表](../#find-your-tool)的第 9 列:在 Tabnine Enterprise
管理介面裡,把 BYO LLM endpoint URL 設成 `http://localhost:9000/v1`。

來源:[Tabnine connect custom LLM](https://docs.tabnine.com/main/software-configuration/connect-custom-llm)。
