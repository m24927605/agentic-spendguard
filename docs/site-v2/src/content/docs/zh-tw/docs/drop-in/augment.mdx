---
title: "在 Augment (BYOK) 接上 SpendGuard"
description: >-
  把 Augment 的 LLM 自訂 endpoint 指到一個跑起來的 SpendGuard egress proxy,
  就能把 SpendGuard 接進去。recipe 還在補,確切的 UI 步驟先看 drop-in 著陸頁。
pagefind: false
---

## 即將推出

完整教學 — 前置需求、確切的 UI 步驟、驗證步驟,還有那些雷 — 會在後續的
slice 補上。在那之前,要設的就是
[drop-in 對照表](../#find-your-tool)的第 13 列:在 Augment BYOK 設定裡,
把 LLM 自訂 endpoint 設成 `http://localhost:9000/v1` 就好。

來源:[Augment BYOK setup](https://docs.augmentcode.com/setup/byok)。
