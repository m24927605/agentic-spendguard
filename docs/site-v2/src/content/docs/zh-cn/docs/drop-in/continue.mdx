---
title: "在 Continue 中接入 SpendGuard"
description: >-
  把 config.yaml 里的 apiBase 指向一个运行中的 SpendGuard egress proxy，即可把
  SpendGuard 接入 Continue。教程还在写，完整 config 块见 drop-in 落地页。
pagefind: false
---

## 即将上线

完整流程——前置条件、确切的 YAML key 路径、验证步骤和那些坑——会在后续 slice 里补上。
在那之前，要配的就是
[drop-in 工具矩阵](../#find-your-tool)里第 3 行的那一项：

```yaml
apiBase: http://localhost:9000/v1
```

来源：[Continue customization → models](https://docs.continue.dev/customization/models)。
